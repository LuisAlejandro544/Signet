feat(security): implementar cifrado en reposo AES-256-GCM y protección de portapapeles

- Implementación de cifrado en reposo para credenciales de Keystores en Room:
  * Crear `com.example.crypto.KeystoreEncryptionManager.kt`: motor de cifrado AES-256-GCM respaldado por `AndroidKeyStore` (y derivación segura en JVM/Desktop) que protege `storePassword` y `keyPassword` con prefijo `enc:v1:`.
  * Actualizar `com.example.data.local.KeystoreEntity.kt`: cifrado transparente al persistir entidades y descifrado al convertir a `KeystoreDetails`, garantizando compatibilidad total hacia atrás con registros previos.
  * Añadir `com.example.crypto.KeystoreEncryptionManagerTest.kt`: suite de pruebas unitarias verificando cifrado, descifrado, manejo de registros heredados y entidades Room.

- Refuerzo de seguridad del portapapeles en UI:
  * Actualizar `com.example.ui.components.details.DetailsActionUtils.kt`: soporte para bandera `ClipDescription.EXTRA_IS_SENSITIVE` (Android 13+ / API 33+) para evitar previsualizaciones inseguras del sistema y desplegar advertencia contextual sobre apps de terceros al copiar secretos.
  * Actualizar `KeystoreCredentialsCard.kt`, `KeystoreBase64Card.kt` y `ApkMatcherSection.kt` para activar la protección de contenido sensible al copiar contraseñas, secretos Base64 y huellas.

- Actualización integral de documentación:
  * `README.md`, `ROADMAP.md`, `AI_CONTEXT.md`, `STRUCTURE.md` y `Changelog-release.md` actualizados para detallar la arquitectura de cifrado en reposo y protección del portapapeles.

---

refactor(crypto): eliminar generador y referencias a exportación .pepk ficticia

- Eliminación completa de componentes criptográficos y UI de .pepk:
  * Eliminar `com.example.crypto.PepkGenerator.kt`: se remueve la implementación ficticia que simulaba la herramienta propietaria pepk.jar de Google Play.
  * Eliminar `com.example.ui.components.details.PepkExportDialog.kt`: se remueve el diálogo modal de exportación .pepk y selección de clave pública.
  * Eliminar `com.example.crypto.PepkGeneratorTest.kt`: se remueven los tests unitarios asociados a la lógica eliminada.

- Limpieza y adaptación del núcleo criptográfico y UI:
  * `SignetBackupManager.kt`: Simplificar `createBackupZip` removiendo los parámetros opcionales y empaquetado de archivos .pepk.
  * `BackupTemplates.kt`: Eliminar referencias a .pepk en las plantillas de `credentials.txt` y `README-BACKUP.txt`.
  * `SnippetGenerator.kt`: Remover la función `generatePepkSnippet` y comando CLI para pepk.jar.
  * `KeystoreHeaderSection.kt`: Eliminar botón de exportación para Google Play (.pepk) y su callback correspondiente.
  * `KeystoreDetailsSheet.kt`: Remover estado reactivo `showPepkDialog` e invocación de `PepkExportDialog`.
  * `KeystoreCodeSnippetsCard.kt`: Remover pestaña `PEPK_CLI` del selector de snippets y sus descripciones asociadas.
  * `WelcomeModels.kt`: Actualizar tarjeta de onboarding número 2 ("Respaldos & CI/CD") enfocándola exclusivamente en respaldos ZIP anti-manipulación, integración con secretos y snippets para Gradle/CI-CD.
  * `SnippetGeneratorTest.kt`: Remover assertions relacionadas con la generación de snippets para pepk.jar.

- Actualización integral de documentación y términos legales:
  * `README.md`: Eliminar sección de exportación .pepk, comandos CLI de pepk.jar y actualizar el resumen de onboarding y términos a 12 secciones.
  * `AI_CONTEXT.md`: Actualizar la descripción de propósito del proyecto y marco legal removiendo referencias a .pepk.
  * `ROADMAP.md`: Remover hitos y tareas relacionadas con el generador .pepk.
  * `STRUCTURE.md`: Actualizar el árbol del proyecto y la descripción de snippets/términos sin archivos ni componentes .pepk.
  * `web/src/pages/privacy.astro` y `web/src/pages/terms.astro`: Remover menciones a archivos .pepk y eliminar la cláusula específica de cifrado PEPK para Google Play, reordenando las secciones legales de 13 a 12.
