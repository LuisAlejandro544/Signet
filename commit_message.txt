feat(onboarding): implementar flujo interactivo de bienvenida, enlaces legales y consentimiento de términos

- Diseñar e implementar `WelcomeScreen.kt` como experiencia de primera ejecución en 4 etapas mediante `HorizontalPager`:
  * Presentación integral de la suite Signet (Generador RSA/EC, contraseñas CSPRNG, validador APK Matcher y respaldos ZIP con firma HMAC).
  * Explicación explícita del modelo de privacidad 100% on-device (sin servidores, sin telemetría ni analíticas).
  * Bloque de consentimiento informado con casilla obligatoria de aceptación de Términos y Condiciones y Política de Privacidad antes de ingresar.
- Integrar gestión reactiva de estado en `KeystoreViewModel` (`isOnboardingCompleted`, `completeOnboarding()`, `resetOnboarding()`) con persistencia en `SharedPreferences`.
- Configurar condicional de navegación en `MainActivity.kt` para mostrar `WelcomeScreen` en el primer inicio y dar paso a `MainScreen` una vez aceptado.
- Enriquecer `SettingsScreen.kt` con la sección oficial "Marco Legal y Políticas de Uso":
  * Acceso directo en el navegador a los Términos y Condiciones (`https://signet-web.luisalejandrososacamacho9.workers.dev/terms/`).
  * Acceso directo a la Política de Privacidad (`https://signet-web.luisalejandrososacamacho9.workers.dev/privacy/`).
  * Botón interactivo para reabrir y repasar la guía explicativa de bienvenida e introducción en cualquier momento.
- Añadir pruebas unitarias en `ExampleRobolectricTest.kt` para validar la transición de estados de onboarding y la consistencia de las constantes de URLs legales.
- Sincronizar y actualizar la documentación en `README.md`, `ROADMAP.md`, `AI_CONTEXT.md` y `STRUCTURE.md`.

refactor(modular): modularización y desacoplamiento de SettingsScreen, SignetBackupManager y ApkMatcher

- Modularizar la interfaz de configuración en el paquete `com.example.ui.screens.settings`:
  * `SettingsHeaderCard.kt`: Tarjeta de encabezado con descripción y estilo M3.
  * `ThemeModeSection.kt`: Selector de modos de pantalla (Claro, Oscuro, Negro 100% AMOLED y Sistema).
  * `ColorPaletteSection.kt`: Selector de acentos cromáticos y soporte para Material You (Android 12+).
  * `LegalLinksSection.kt`: Botones de acceso al marco jurídico web y relanzamiento de la guía de bienvenida.
  * `DistributionInfoSection.kt`: Metadatos de licencia GPL v3, modo offline y compatibilidad con tiendas APK.
  * Simplificar `SettingsScreen.kt` como orquestador limpio y conciso.
- Desacoplar el subsistema de respaldos y verificación criptográfica en `com.example.crypto.backup`:
  * `BackupTemplates.kt`: Generación modular de `credentials.txt`, `key.properties` y `README-BACKUP.txt`.
  * `BackupIntegrityVerifier.kt`: Cálculo de hash SHA-256, firma HMAC-SHA256 y verificación estricta anti-manipulación.
  * `SignetBackupManager.kt`: Fachada orquestadora para compresión y descompresión ZIP atómica.
- Desacoplar el motor forense de análisis de APK en `com.example.crypto.apk`:
  * `ApkSigningBlockParser.kt`: Extracción directa de certificados para esquemas v2 y v3 en el APK Signing Block.
  * `ApkV1SignatureParser.kt`: Extracción de certificados PKCS#7 en `META-INF/` mediante BouncyCastle CMS.
  * `AxmlManifestParser.kt`: Extracción rápida de `packageName` desde el string pool binario de AndroidManifest.xml.
  * `ApkMatcher.kt`: Fachada orquestadora de análisis y cruce de huellas con keystores.
- Actualizar `STRUCTURE.md` con el nuevo árbol de archivos modular y flujo de responsabilidades.
