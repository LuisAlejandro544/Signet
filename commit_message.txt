feat(ci): agregar workflow de validación cruzada Signet con keytool y Google apksigner

- Implementar `.github/workflows/cli-interoperability-test.yml` con ejecución manual (`workflow_dispatch`)
- Crear suite de validación `scripts/cli_interoperability_test.py` para probar compatibilidad entre Keystores de Signet y herramientas oficiales de terminal
- Validar generación con `keytool -genkeypair`, extracción de huellas SHA-256 (`keytool -list`) y firma de APKs con `apksigner` de Google SDK
- Verificar conformidad con los esquemas de firma de Android v1 (JAR), v2 (APK Signature) y v3
- Generar reporte estructurado `cli-interop-report.json` con enmascaramiento estricto y despacho confidencial a Telegram
- Actualizar documentación en README.md, ROADMAP.md, AI_CONTEXT.md y STRUCTURE.md
